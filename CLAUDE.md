# Claude 开发约定

## 项目定位

AirborneDiag是面向民机机载系统的智能故障诊断工程，当前原型聚焦MPC5554、TMS320F28335等MCU的片上资源及外设故障。

- 项目简介与当前范围：见 README.md。
- 目录职责与架构规划：见 docs/architecture.md。
- 文档维护规则：见 .claude/rules/documentation-rules.md。

本文件只记录开发约定，不重复维护详细架构和业务设计。

## 上下文读取

1. 根据用户提供的任务背景、范围和验收标准开展工作，不假定能够访问GitHub Issue。
2. 按任务需要读取相关文档、代码和测试，不默认通读整个工程。
3. 可以主动搜索文件、查看目录及继续读取相关依赖，不需要为常规只读操作逐次请求确认。
4. 不默认读取模型权重、大型原始资料、生成物和无关历史记录。
5. 文档用于理解设计和定位代码；实施修改前仍需核对相关实现及测试。
6. 需求存在歧义、方案涉及重大取舍或操作超出任务范围时，先说明问题并请求确认。

## 开发约定

1. 先检查现有实现，再提出与任务规模相称的简短计划。
2. 只完成当前任务，不顺手重构、扩展功能或引入无关依赖。
3. 优先使用简单、可验证的实现；仅在实际需要时增加模块和抽象。
4. 保留用户已有修改，不覆盖或撤销不属于本次任务的变更。
5. 未明确的接口、配置和业务判据应标记为待确认，不编造成既有事实。
6. 调试时先收集证据、定位原因，再进行针对性修复。
7. 文档与代码不一致时，指出差异并结合任务目标判断处理方式，不静默认定某一方必然正确。

## 诊断领域约束

1. 芯片型号、修订版本、寄存器定义和故障判据必须有可核对的依据。
2. 不将不同MCU的同名外设、寄存器或状态定义直接混用。
3. 模拟案例与真实设备记录必须明确区分。
4. 缺失数据表示未知，不得自动解释为正常、零值或检测通过。
5. 区分观测事实、规则确认的状态和待验证的候选根因。
6. 不把模型自行生成的数值当作经过校准的诊断概率。
7. 当前原型输出分析及处理建议，不自动执行设备控制操作。

## 验证与汇报

1. 根据变更选择必要的检查、测试或运行验证。
2. 目录和纯文档变更检查结构、链接及内容一致性，不为占位文件编写单元测试。
3. 涉及诊断逻辑的变更，应使用有明确预期结果的案例验证。
4. 无法执行验证时，说明未验证的内容及原因。
5. 模拟接口通过不等于真实模型调用通过，生成报告成功不等于诊断正确。
6. 任务结束后简要汇报修改内容、实际验证结果和遗留问题。
7. 按文档维护规则更新受影响的说明，不机械追加开发日志。

## Git与协作边界

默认在用户准备好的本地分支中工作。未经用户明确授权，不执行以下操作：

- 创建或切换分支；
- commit、push或强制push；
- 创建、合并或关闭Pull Request；
- 关闭Issue或修改Project状态；
- 修改仓库权限、分支保护及Project权限。

如果当前分支或工作区状态不适合执行任务，先说明情况，不自行切换分支或清理已有修改。