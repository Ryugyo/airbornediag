# 首批诊断场景

本文件定义首批诊断场景及其判据依据。字段语义与格式规则见 [contracts.md](contracts.md)。

## 范围与来源

- **芯片**：MPC5554。寄存器定义与判据均出自 [MPC5554_RM](../knowledge/source-registry.json)，即 MPC5553/5554 Microcontroller Reference Manual（MPC5553_MPC5554_RM，Rev. 5）。
- **外设**：FlexCAN2（第 22 章）与 DSPI（第 20 章）。
- **场景来源**：**按手册构造的模拟场景**，不含真实控制器记录。对应记录的 `origin` 均为 `simulated`。
- 本批不覆盖 TMS320F28335：`knowledge/sources/` 中尚无其资料，不得借用 MPC5554 的同名外设定义。

## 依据核对情况

以下条目已对照手册原文核对，作为场景判据的来源：

| 位置 | 内容 |
|---|---|
| 22.3.3.2，Table 22-8 | `CANx_CR` 的 `LOM`、`BOFFMSK`、`BOFFREC` 及位定时字段 `PRESDIV`/`PROPSEG`/`PSEG1`/`PSEG2`/`RJW`；`BOFFREC` 为 1 时禁用总线关闭自动恢复 |
| 22.3.3.5，Figure 22-7 | `CANx_ECR` 的 `TXECTR`/`RXECTR`，总线状态迁移规则与总线关闭期间的语义变化 |
| 22.3.3.6，Figure 22-8，Table 22-11 | `CANx_ESR` 的错误标志位（读清除）与状态位，`FLTCONF` 编码为 00 错误主动 / 01 错误被动 / 1X 总线关闭 |
| 20.3.2.4，Figure 20-6，Table 20-6 | `DSPIx_SR` 的 `TCF`/`TXRXS`/`EOQF`/`TFUF`/`TFFF`/`RFOF`/`RFDF` 与 FIFO 计数器 |

三点说明：

1. **未使用页码定位。** 该手册的偶数页使用页眉、奇数页使用页脚，PDF 文本层未保留完整的页边界，页码无法逐条核实。因此 `basis.locator` 使用章节号与表/图编号。
2. **未写入位编号。** 本任务不实现寄存器解析，场景与契约只使用字段名与表/图编号。具体位编号留待解析实现时对照 Figure 与 Table 逐条核对。
3. 手册随附 Addendum Rev. 3 的勘误只涉及 Table 2-1 与 Table 2-2 的 VDDEH10 引脚定义，未涉及第 20 章与第 22 章，本批引用不受影响。

## 场景清单

| 场景 | 外设 | 触发证据 | 示例 |
|---|---|---|---|
| FC-01 总线关闭 | FlexCAN2 | `CANx_ESR` 的 `FLTCONF` 为 1X | `REC/RPT-2026-0918-002` |
| FC-02 错误被动与只听模式歧义 | FlexCAN2 | `CANx_ESR` 的 `FLTCONF` 为 01 | **未配，后续待实现示例** |
| DS-01 DSPI 自检失败且无证据 | DSPI | `test.result` 为 fail 且无寄存器观测 | `REC/RPT-2026-0918-001` |
| DS-02 `TFUF` 与工作模式自相矛盾 | DSPI | `TFUF` 置位且 `MSTR` 为主机模式 | **未配，后续待实现示例** |

FC-02 与 DS-02 已确定判据，但本批**不提供示例文件**，属于后续待实现示例。未配示例的后果是：`scripts/validate_contracts.py` 不覆盖这两条判据，契约测试也不覆盖它们。

## FC-01 总线关闭

**证据**：`CANx_ESR` 的 `FLTCONF` 为 1X。可能同时观察到 `BOFFINT` 置位、`CANx_ECR` 的 `TXECTR` 取值。

**可确认的结论**

1. 采集时刻模块处于总线关闭状态。依据 22.3.3.6 的 `FLTCONF` 编码。该结论**不需要** `CANx_CR` 佐证：`LOM` 只会把 `FLTCONF` 伪造成「错误被动」，不会伪造成总线关闭。
2. 总线关闭期间 `TXECTR` 的语义已改变。依据 22.3.3.5：进入总线关闭时 `TXECTR` 复位为零，随后与内部计数器级联，用于计数总线上「11 个连续隐性位」的出现次数，达到 128 次时更新 `FLTCONF`。因此此时的 `TXECTR` **不是**发送错误计数，不能用于衡量历史错误严重程度。
3. 若 `CANx_CR` 的 `BOFFREC` 为 1，则不会自动恢复。依据 Table 22-8：该位为 1 时禁用自动恢复，模块保持总线关闭直至该位被清除。
4. 进入总线关闭前的错误累积不可能仅来自 ACK 错误。依据 22.3.3.5：总线关闭要求 `TXECTR` 超过 255；而在只有单个节点运行的情形下，ACK 错误在模块进入错误被动后不再推高 `TXECTR`，手册据此说明该情形下「设备永远不会进入总线关闭」。

**不可推出的结论**

- **不得据此排除「总线上只有一个节点」。** 上述第 4 条的适用前提是「错误源仅为无应答」。单节点总线若同时存在位错误、填充错误等其他错误源，`TXECTR` 仍可持续累积并进入总线关闭。该结论只排除「错误源仅为无应答」这一条路径。
- 不得推断总线关闭发生的时刻，也不得推断进入总线关闭前各错误类型的分布。
- 不得由 `TXECTR` 的当前取值推断距离恢复还有多久：该值是瞬时采样，且级联计数器的相位未知；`BOFFREC` 为 1 时即使计数达到 128 也不会恢复为错误主动。
- 不得判断异常位于本节点还是总线上的其他节点。
- 不得给出终端电阻、差分电平等物理层的具体期望数值。`knowledge/sources/` 中没有总线规范，这类判据缺依据，只能作为待核实建议，并在建议中注明期望值须依据适用规范确认。

**常见缺失信息**

总线电气测量、`CANx_ESR` 错误标志位在上一次被读取的时刻（决定读清除位的覆盖区间）、`CANx_CR` 的位定时配置与总线约定波特率。

## FC-02 错误被动与只听模式歧义

**证据**：`CANx_ESR` 的 `FLTCONF` 为 01。

**判据与歧义**：依据 22.3.3.6，`FLTCONF` 为 01 表示错误被动；但同处说明，若 `CANx_CR` 的 `LOM` 置位，`FLTCONF` 会被强制显示为「错误被动」。因此：

- 记录中**缺少** `CANx_CR` 的 `LOM` 时，无法区分真实的错误被动与只听模式，应判为不可判定。
- 记录中 `LOM` 为 0 时，可确认模块处于错误被动。

**本场景未配示例，属于后续待实现示例。** 它存在的价值是：**同一个字段取值在不同配置下含义不同**，契约必须允许把「配置未知」如实表达为未知，而不是二选一。在补上示例之前，这条判据只停留在文档中，未被任何用例或校验覆盖。

## DS-01 DSPI 自检失败且无证据

**证据**：检测报失败，但没有 DSPI 的寄存器观测。

**为什么结论只能是「不可判定」**：第 20 章没有错误处理或故障封闭章节，`DSPIx_SR` 只提供传输完成、FIFO 计数与 FIFO 溢出/下溢标志。这些状态不足以把一次自检失败归入某个故障状态，也不足以在「传输未启动」「传输完成但数据不符」「接收缓冲异常」之间区分。

因此本场景的报告形态是：`fault_state` 与 `root_cause` 均为 `not_determined`，`confirmed_states` 与 `candidate_causes` 为空，全部内容集中在 `insufficient_data` 与 `recommended_checks`。**候选原因为空是结论，不是遗漏**：仅凭一条自检失败给出根因，属于 CLAUDE.md 明确禁止的编造判据。

## DS-02 `TFUF` 与工作模式自相矛盾

**证据**：`DSPIx_SR` 的 `TFUF` 置位，同时 `DSPIx_MCR` 的 `MSTR` 置位（主机模式）。

**判据**：依据 Table 20-6，发送 FIFO 下溢**仅对工作在 SPI 从模式的 DSPI 检测**。主机模式下出现 `TFUF` 属于输入自相矛盾。

**本场景未配示例，属于后续待实现示例。**

本场景的处置不是据此诊断，而是写入报告的 `inconsistencies`，并说明矛盾所在；在矛盾澄清之前，不应基于该记录给出任何 DSPI 结论。

## 支持范围

**本批支持的结论**

- FlexCAN2 的总线关闭状态、错误被动状态（配置已知时）、总线关闭期间的 `TXECTR` 语义、`BOFFREC` 对恢复行为的影响。
- 由 `CANx_ESR` 读清除位构成的错误标志快照，且其覆盖区间已知时。
- 上述结论的适用边界与不可推出项，按场景逐条列出。

**本批明确不支持**

- 物理层成因定位。缺少总线规范依据，只能给出待核实建议。
- DSPI 的故障状态确认。第 20 章无对应判据。
- 总线关闭发生前的错误类型分布。
- TMS320F28335 的任何场景。资料缺失。
- 任何根因的确认（`root_cause` 首版不开放 `confirmed`）。

未列入支持范围的故障类型应明确报告为不支持，不得默认为正常。
